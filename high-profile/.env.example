# Omit for local, disk-persistent embedded PostgreSQL.
# DATABASE_URL=postgresql://high_profile:change-me@localhost:5432/high_profile
PORT=4310
HOST=127.0.0.1
SEED_FIXTURES=true
# Required for review mutations. Never put this in the browser bundle.
# REVIEW_TOKEN=replace-with-a-long-random-secret
